/**
 * Figma fidelity note: This page preserves the supplied reference as a single
 * centered 200 × 100 px translucent rectangle, with no added visual content.
 */
export default function Home() {
  return (
    <main className="figma-stage" aria-label="LifeLink visual component">
      <div className="figma-rectangle" aria-hidden="true" />
    </main>
  );
}
